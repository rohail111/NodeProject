<html>
<head></head>
<body>
    <h1> Following are the titles of given websites: </h1>
    <ul>
        <% for(var i=0; i < url.address.length; i++) {%>
            <li>
                <%= url.address[i]%> - <%= url.title[i] %>
            </li>
        <% } %>
    </ul>
</body>
</html>